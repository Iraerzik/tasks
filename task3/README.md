# Task3

1. functions.c
Реализует функции:
```
void clearlist();
```
функция очищает глобальный список
```
void null_list();
```
функция создает нулевой список и устанавливает переменные на ноль
```
void termlist();
```
функция подравнивает память для списка
```
void insertsortlist();
```
функция сортирует список
```
void nullbuf();
```
функия создает нулевую буферную переменную
```
void addsym();
```
функция добавляет символ в буферную переменную
```
void addword();
```
функция добавляет сформированное слово в список
```
void printlist();
```
функция печатает список
```
int symset(int c);
```
функция проверяет спецсимвол
2. main.c
использует оператор switch и при встрече разных символов переход в нужный уровень и использует необходимые функции
